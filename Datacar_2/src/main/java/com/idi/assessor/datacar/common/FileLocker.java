package com.idi.assessor.datacar.common;

import java.io.File;

// Stub interface - replace with your actual implementation
public interface FileLocker {

    boolean lock(File file);
    // Add other methods like unlock if they exist
}
